{
  "name": "code-editor",
  "version": "1.0.0",
  "description": "A web-based code editor with side-by-side output.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
